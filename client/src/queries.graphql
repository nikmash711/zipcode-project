query ZipInformation($country: String!, $zipCode: String!) {
  zipInformation(country: $country, zipCode: $zipCode) {
    state
    city
  }
}

query countries {
  countries {
    name
    code
  }
}
